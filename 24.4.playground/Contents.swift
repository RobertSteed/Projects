import UIKit

var greeting = "Hello, playground"

func likedSpots(arr: [Int], liker: Int) -> Int {
        guard let arr > 0,
          guard let liker > 0 else { return }
    var myEvenAnswerArray: [Int] = []
    var myOddAnswerArray: [Int] = []
    for i in arr {
        if i % 2 = 0 {
            myEvenAnswerArray.append(i)
        } else {
            myOddAnswerArray.append(i)
        }
      
    }
    
    
    
    return 0
}


