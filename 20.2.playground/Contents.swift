import UIKit

var greeting = "Hello, playground"


func multiples(start: Int, end: Int) -> Int? {
    guard start > 0 && end > 0 else {return nil}
    if start.isMultiple(of: end) {
        
        
    }
    return 0
}

