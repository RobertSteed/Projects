import UIKit

var greeting = "Hello, playground"

func splittingCases(str: String, cases: Int) -> [String] {
    guard (str.count / cases) > 0 else { return nil }
    var splitWord: [String] = []
    
    
    return []
}
