import UIKit

var greeting = "Hello, playground"

func isItToday(date: Date) -> Bool {
    var now: Date { get } {
        if now == date {
            return true
        } else {
            return false
        }
        return true
    }
}
