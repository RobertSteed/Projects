import UIKit

var greeting = "Hello, playground"


func add(numbers: [Int]) -> Int {
    let result = numbers
    var total = [Int] ()
    for index in result {
        return total + result.count
    }
}
var apples = add(numbers: [4])
print(apples)









