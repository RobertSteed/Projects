import UIKit

var greeting = "Hello, playground"

func countSentences(paragraph: String) -> Int {
    var sentences: [String] = []
    input.enumeratesSubstrings(in: input.startIndex..., options: .bySentences) { (string, range, enclosingRamge, stop) in sentences.append(string!)
    }
    }
    if paragraph.includes(".")
    return 0
}

