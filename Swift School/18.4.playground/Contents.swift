import UIKit

var greeting = "Hello, playground"


func checkingUpperCase(first: Character, second: Character) -> Int? {
    if first.uppercased() && second.uppercased {
        return 1
    } else if first.lowercased() && second.lowercased() {
        return 1
    } else if first.uppercased() 
    }
    
    return 0?
}
