import UIKit

var greeting = "Hello, playground"



func sortDifferencesThenSum(first: [Int]) -> Int {
    var placeholderArray = first
    var newArray = placeholderArray.sort(by: >)
    
    
    return 0
}

